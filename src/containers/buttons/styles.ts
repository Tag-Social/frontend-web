import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    buttonIcon: {
        marginRight: '4px',
    },
    listItemIcon: {
        minWidth: '30px',
    },
}));
